#!/bin/bash
       read x
       read y
            if (( x > y ));
then
                 echo "$x is greater than $y"
                 else
fi
                      if (( x = y ));
then
                            echo "$x is equal to $y"
		    else
                                if (( x < y ));
then
                                     echo "$x is less than $y"
                                     fi
          
fi
     
done
done
